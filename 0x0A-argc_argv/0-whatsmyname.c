#include <stdio.h>

/**
 * main - print the program name, and new line
 * @argc: Argument count/number.
 * @argv: Argument vector , array of pointr to the argument.
 *
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n",argv[0]);

	return(0);
}
